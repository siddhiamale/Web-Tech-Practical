4. Write middleware logic that allows access only if auth()->user()->role === 'admin'.

public function handle($request, Closure $next)
{
    if(auth()->user()->role === 'admin'){
        return $next($request);
    }

}
