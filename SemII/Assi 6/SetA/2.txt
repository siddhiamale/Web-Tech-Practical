2. Write Blade code using @if ($errors->any()) to display all validation errors as a bulleted list.

@if($errors->any())
<ul>
@foreach($errors->all() as $error)
<li>{{ $error }}</li>
@endforeach
</ul>
@endif
