6. Write a route group that protects /admin/* routes using auth and admin middleware.

Route::middleware(['auth','admin'])->group(function(){
Route::get('/admin/dashboard',function(){
return view('admin.dashboard');
});
});
